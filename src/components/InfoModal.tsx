
import React from 'react';

interface InfoModalProps {
    onClose: () => void;
}

const InfoModal: React.FC<InfoModalProps> = (props) => {
    return <div>Info Modal Placeholder</div>;
};

export default InfoModal;
